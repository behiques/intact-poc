// Shared hook utilities - bulletproof pattern for reusable logic
export { useLocalStorage } from './useLocalStorage'
export { useDebounce } from './useDebounce'

// This file serves as the entry point for shared hooks
// Individual hooks should be imported directly to avoid barrel export issues
