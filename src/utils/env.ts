/**
 * Environment Variables
 *
 * Direct access to environment variables without validation.
 * Use process.env directly throughout the application.
 */

// Re-export process.env for backwards compatibility if needed
export const env = process.env
